server:
  port: 8080
spring:
  application:
    name: application
  cloud:
    config:
      uri: http://ms-config-server-toto.apps.oc.smatine.com
      failFast: true
      retry:
        initialInterval: 2000
        maxAttempts: 30
